namespace __MOD_ID__.__MOD_ID__Code;

/// <summary>
/// Placeholder for game-independent logic.
///
/// Anything that does not touch Godot or sts2 types belongs in files like this one, so
/// it can be linked into the test project and covered without launching the game.
/// Delete this file once the mod has real logic — and update the Compile Include in
/// __MOD_ID__.Tests/__MOD_ID__.Tests.csproj when you do.
/// </summary>
internal static class Example
{
    public static int Double(int value) => value * 2;
}
